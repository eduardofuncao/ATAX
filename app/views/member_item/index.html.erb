<p id="notice"><%= notice %></p>

<h1>Produtos / <%= link_to 'Serviços', member_order_index_path %></h1>
<table>
  <thead>
    <tr>
      <th>Name</th>
      <th>Category</th>
      <th>Quantity</th>
      <th>Description</th>
      <th>Price</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @items.each do |item| %>
      <tr>
        <td><%= item.name %></td>
        <td><%= item.category %></td>
        <td><%= item.quantity %></td>
        <td><%= item.description %></td>
        <td><%= item.price %></td>
        <td><%= link_to 'Mostrar', item %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>
